# 🗺️ Google Places API - Инструкция по настройке

## Шаг 1: Создать проект в Google Cloud Console

1. Открой [Google Cloud Console](https://console.cloud.google.com/)
2. Войди в свой Google аккаунт
3. Нажми **"Select a project"** → **"New Project"**
4. Введи название: **TripBuddyAI**
5. Нажми **"Create"**

---

## Шаг 2: Включить Places API

1. В меню слева выбери **"APIs & Services"** → **"Library"**
2. Найди **"Places API"** (используй поиск)
3. Нажми на **Places API**
4. Нажми **"Enable"**
5. Также включи:
   - **Places API (New)** (рекомендуется)
   - **Maps JavaScript API** (для карт)
   - **Geocoding API** (для адресов)

---

## Шаг 3: Создать API ключ

1. В меню слева выбери **"APIs & Services"** → **"Credentials"**
2. Нажми **"+ CREATE CREDENTIALS"** → **"API key"**
3. Скопируй созданный ключ (выглядит как `AIzaSyB...`)
4. Нажми **"Restrict Key"** (важно для безопасности!)

---

## Шаг 4: Настроить ограничения API ключа

### Вкладка "API restrictions":
1. Выбери **"Restrict key"**
2. Отметь:
   - ✅ Places API
   - ✅ Places API (New)
   - ✅ Maps JavaScript API
   - ✅ Geocoding API
3. Нажми **"Save"**

### Вкладка "Application restrictions" (опционально):
Для тестирования можно оставить **"None"**, но для прода:
1. Выбери **"IP addresses"**
2. Добавь IP адрес твоего сервера

---

## Шаг 5: Добавить ключ в проект

1. Скопируй `.env.example` в `.env`:
```bash
cd /Users/vladyslav/Desktop/js/tripgid/TripBuddyAI/backend
cp .env.example .env
```

2. Открой `.env` и вставь свой ключ:
```env
GOOGLE_PLACES_API_KEY=AIzaSyB_твой_реальный_ключ_здесь
```

3. **ВАЖНО:** `.env` файл уже в `.gitignore` — не коммить его!

---

## Шаг 6: Настроить биллинг (обязательно!)

Google Places API требует привязать карту, **НО**:
- ✅ **$200/месяц** бесплатных кредитов
- ✅ Первые **100,000 запросов/месяц** бесплатно
- ✅ Для тестов и MVP этого достаточно

### Как добавить карту:
1. В меню выбери **"Billing"**
2. Нажми **"Link a billing account"**
3. Создай новый аккаунт или выбери существующий
4. Введи данные карты

**Не переживай:** Списания не будет, пока не превысишь лимиты!

---

## Шаг 7: Проверить работу API

1. Запусти backend:
```bash
cd /Users/vladyslav/Desktop/js/tripgid/TripBuddyAI/backend
npm run dev
```

2. Проверь статус в логах:
```
✅ [GooglePlaces] API working! Found XX places
```

3. Или открой в браузере:
```
http://localhost:3001/api/status
```

Должен показать:
```json
{
  "google_places": {
    "working": true,
    "message": "API working! Found 20 places"
  }
}
```

---

## 🎯 Быстрая проверка (без запуска сервера)

Проверь API ключ через curl:
```bash
curl "https://maps.googleapis.com/maps/api/place/nearbysearch/json?location=48.8566,2.3522&radius=1000&type=restaurant&key=ТВОЙ_КЛЮЧ"
```

Если работает — увидишь JSON с ресторанами Парижа!

---

## 🚨 Частые ошибки

### ❌ `PERMISSION_DENIED`
**Решение:** Включи Places API в библиотеке (шаг 2)

### ❌ `REQUEST_DENIED - billing not enabled`
**Решение:** Привяжи карту в Billing (шаг 6)

### ❌ `INVALID_REQUEST`
**Решение:** Проверь формат координат: `lat,lng` (например: `48.8566,2.3522`)

### ❌ `OVER_QUERY_LIMIT`
**Решение:** Превышен лимит запросов. Проверь биллинг или жди сброса квоты

---

## 📊 Мониторинг использования

1. В Google Cloud Console → **"APIs & Services"** → **"Dashboard"**
2. Смотри графики запросов к Places API
3. Следи за бюджетом в **"Billing"** → **"Reports"**

---

## 💡 Советы

1. **Для разработки:** Один ключ для localhost
2. **Для прода:** Отдельный ключ с IP ограничениями
3. **Кэширование:** Сохраняй результаты Places API в базе, чтобы снизить количество запросов
4. **Rate Limiting:** Ограничь количество запросов от одного пользователя

---

## ✅ Готово!

После настройки API ключа твоё приложение будет получать **реальные места** из любого города мира! 🌍

