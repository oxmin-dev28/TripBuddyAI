# 🔧 Исправление ошибки "Property 'Shadows' doesn't exist"

## Причина ошибки
Ошибка возникает из-за старого кэша Metro bundler, который не видит обновлённый файл `theme.ts`.

## ✅ Решение

### Вариант 1: Используй готовый скрипт (РЕКОМЕНДУЕТСЯ)

Я создал скрипт для автоматической очистки кэша и запуска:

```bash
cd TripBuddyAI
./start-app.sh
```

Скрипт сделает:
- ✅ Остановит все процессы Expo
- ✅ Очистит `.expo` и `node_modules/.cache`
- ✅ Запустит Expo с `--clear`

**⚠️ Важно:** Когда Expo спросит про Expo account, выбери **"Proceed anonymously"**

---

### Вариант 2: Ручной запуск

```bash
cd TripBuddyAI

# 1. Убить все процессы
pkill -f "expo start"
pkill -f "metro"

# 2. Очистить кэш
rm -rf .expo node_modules/.cache

# 3. Запустить
npx expo start --clear
```

Когда спросит про account - нажми **"Proceed anonymously"**

---

### Вариант 3: Полная переустановка (если не помогло)

```bash
cd TripBuddyAI

# Удалить все
rm -rf node_modules .expo

# Переустановить
npm install

# Запустить
npx expo start --clear
```

---

## 🎯 Проверка

После запуска ты должен увидеть:

✅ **QR-код** для сканирования  
✅ **"Metro waiting on exp://..."**  
✅ **НЕТ ошибок** про Shadows  

Если увидишь логи:
```
LOG  🌐 [API] Development: Using host IP: 192.168.1.100
LOG  🔗 [API] Base URL: http://192.168.1.100:3001/api
```

**Без** строки:
```
ERROR  [runtime not ready]: ReferenceError: Property 'Shadows' doesn't exist
```

Значит всё работает! 🎉

---

## 📱 Как открыть приложение

### iOS (iPhone):
1. Открой **Camera** app
2. Наведи на QR-код
3. Нажми на уведомление

### Android:
1. Установи **Expo Go** из Play Store
2. Открой Expo Go
3. Отсканируй QR-код

### Simulator:
- `i` - iOS Simulator (нужен Xcode)
- `a` - Android Emulator (нужен Android Studio)
- `w` - Web browser (работает сразу)

---

## 💡 Почему это происходит?

Metro bundler (сборщик React Native) кэширует файлы для быстрой работы. Когда мы добавили новые файлы (`AuthScreen`, `LeaderboardScreen`, etc.), которые импортируют `Shadows`, старый кэш не обновился.

Флаг `--clear` заставляет Metro пересобрать всё с нуля.

---

## 🆘 Всё ещё не работает?

Напиши мне полный вывод команды:
```bash
cd TripBuddyAI
npx expo start --clear 2>&1 | head -50
```

И я помогу! 🚀

