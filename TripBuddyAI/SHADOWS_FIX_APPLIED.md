# ✅ ИСПРАВЛЕНО: Property 'Shadows' doesn't exist

## Проблема была найдена! 🎯

В файле `theme.ts` была **циклическая зависимость**:

```typescript
// ❌ БЫЛО (неправильно):
export const Shadows = {
  sm: {
    shadowColor: Colors.shadowColor,  // ← Зависит от Colors!
    ...
  },
}
```

Когда React Native/Hermes пытался инициализировать модуль, он не мог правильно определить порядок:
- `Shadows` ссылается на `Colors.shadowColor`
- Но `Colors` ещё не был полностью инициализирован

## ✅ Исправление

Вынес `shadowColor` в отдельную константу:

```typescript
// ✅ СЕЙЧАС (правильно):
const SHADOW_COLOR = 'rgba(0, 0, 0, 0.1)';

export const Colors = {
  ...
  shadowColor: SHADOW_COLOR,
}

export const Shadows = {
  sm: {
    shadowColor: SHADOW_COLOR,  // ← Независимо!
    ...
  },
}
```

---

## 🚀 Как применить исправление

Файл `theme.ts` уже обновлён. Теперь просто:

### В терминале где запущен Expo, нажми:

```
r
```

Это перезагрузит приложение с новым кодом.

### Или перезапусти Expo:

```bash
# Ctrl+C чтобы остановить
# Затем:
npx expo start
```

---

## 🎉 Теперь должно работать!

После перезагрузки ошибка `Property 'Shadows' doesn't exist` исчезнет.

Все новые экраны загрузятся корректно:
- ✅ AuthScreen
- ✅ LeaderboardScreen  
- ✅ SelectCityScreen
- ✅ PlaceSelectionScreen
- ✅ И все остальные

---

## 💡 Урок на будущее

В React Native с Hermes нужно избегать:
- Циклических зависимостей между константами
- Ссылок на свойства объектов до их полной инициализации

Используйте промежуточные константы когда нужно переиспользовать значения! 🎯

