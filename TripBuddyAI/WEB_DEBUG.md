# 🔥 СРОЧНОЕ РЕШЕНИЕ

## Попробуй запустить в WEB версии!

В терминале Expo нажми:

```
w
```

Это откроет приложение в браузере. В браузере:
1. Открой DevTools (F12)
2. Посмотри в Console какая именно ошибка

Web версия **не кэширует** так агрессивно как iOS.

---

## Если в WEB тоже ошибка

Значит проблема в самом импорте. Попробуй:

1. **Закрыть полностью** Expo (Ctrl+C)

2. **Удалить проблемные файлы** (новые, которые я создал):

```bash
cd TripBuddyAI

# Временно переименовать новые экраны
mv src/screens/auth src/screens/auth.backup
mv src/screens/main/LeaderboardScreen.tsx src/screens/main/LeaderboardScreen.tsx.backup
mv src/screens/main/SelectCityScreen.tsx src/screens/main/SelectCityScreen.tsx.backup
mv src/screens/main/PlaceSelectionScreen.tsx src/screens/main/PlaceSelectionScreen.tsx.backup
```

3. **Запустить:**

```bash
npx expo start
```

4. **Проверить** - если ошибка исчезла, значит проблема в новых файлах

---

## Попробуй СЕЙЧАС:

Нажми `w` в терминале Expo и посмотри что в браузере!

