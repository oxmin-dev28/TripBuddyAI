# 🗺️ Dynamic Viewport Loading

## ✅ Что реализовано

### Умная подгрузка мест в зависимости от viewport:

1. **Динамический радиус**
   - Zoom in (приближение) → меньший радиус → больше деталей
   - Zoom out (отдаление) → больший радиус → обзор области

2. **Автоматическая подгрузка**
   - Пользователь двигает карту → загружаются новые места
   - Пользователь меняет zoom → пересчитывается радиус

3. **Кеширование регионов**
   - Уже загруженные области не перезагружаются
   - Места накапливаются при исследовании карты

---

## 🧮 Как работает

### 1. Расчёт радиуса от zoom:

```typescript
const calculateRadiusFromDelta = (latitudeDelta: number): number => {
  // latitudeDelta - вертикальный размер viewport
  // 1 градус широты ≈ 111 км
  const kmPerDegree = 111;
  const visibleKm = latitudeDelta * kmPerDegree;
  const radiusKm = visibleKm * 0.6; // 60% от видимой области
  
  // Ограничиваем: от 1 км до 50 км
  return Math.min(Math.max(radiusKm * 1000, 1000), 50000);
};
```

**Примеры:**
- `latitudeDelta = 0.01` → ~1.1 км видимо → радиус = **660 м**
- `latitudeDelta = 0.05` → ~5.5 км видимо → радиус = **3.3 км**
- `latitudeDelta = 0.2` → ~22 км видимо → радиус = **13 км**
- `latitudeDelta = 1.0` → ~111 км видимо → радиус = **50 км** (max)

### 2. Кеширование регионов:

```typescript
const getRegionKey = (lat, lng, radius): string => {
  // Округляем до 3 знаков (≈100м точность)
  const latRounded = Math.round(lat * 1000) / 1000;
  const lngRounded = Math.round(lng * 1000) / 1000;
  const radiusKm = Math.round(radius / 1000);
  
  return `${latRounded},${lngRounded},${radiusKm}`;
  // Например: "50.450,30.523,5"
};
```

**Логика:**
- Каждый регион получает уникальный ключ
- Если ключ уже в `loadedRegions` → пропускаем загрузку
- Если новый → загружаем и добавляем в Set

### 3. Аккумуляция мест:

```typescript
// Первая загрузка
fetchPlaces('all', location1, 5000, false); // Replace mode
places = [1, 2, 3, 4, 5] // 5 мест

// Пользователь сдвинул карту
fetchPlaces('all', location2, 5000, true);  // Append mode
places = [1, 2, 3, 4, 5, 6, 7, 8] // +3 новых места

// Удаляются дубликаты по ID
```

---

## 🎮 Пользовательский опыт

### Scenario 1: Zoom Out (отдаление)

```
1. Пользователь на карте Киева
   Zoom: latitudeDelta = 0.01
   Радиус: 660м
   Места: 50 мест в центре

2. Пользователь отдаляется (zoom out)
   Zoom: latitudeDelta = 0.1
   Радиус: 6.6 км
   → Автоматически загружаются места в радиусе 6.6 км
   Места: 50 + 200 = 250 мест

3. Ещё больше отдаление
   Zoom: latitudeDelta = 0.5
   Радиус: 33 км
   → Загружаются места всего города
   Места: 250 + 500 = 750 мест
```

### Scenario 2: Pan (перемещение)

```
1. Центр: 50.450, 30.523
   Радиус: 5 км
   Регион: "50.450,30.523,5" ✅ Загружено
   Места: 320

2. Сдвинули на 2 км западнее: 50.450, 30.493
   Радиус: 5 км
   Регион: "50.450,30.493,5" ❌ Новый!
   → Загружаем места
   Места: 320 + 150 = 470 (убрали дубликаты)

3. Вернулись назад: 50.450, 30.523
   Регион: "50.450,30.523,5" ✅ Уже загружен
   → Пропускаем загрузку
   Места: 470 (без изменений)
```

---

## 📊 Производительность

### Оптимизации:

1. **Debouncing не нужен**
   - `onRegionChangeComplete` срабатывает только после остановки
   - Не вызывается при плавной анимации

2. **Кеширование регионов**
   - Каждый регион загружается только 1 раз
   - Set операции O(1)

3. **Append вместо Replace**
   - Новые места добавляются к существующим
   - Дубликаты отфильтровываются по ID

4. **Ограничение радиуса**
   - Min: 1 км (детализация)
   - Max: 50 км (не перегружаем API)

---

## 🔧 API Changes

### Backend (`/backend/src/routes/places.ts`):

Уже поддерживает параметр `radius`:

```typescript
router.get('/', async (req, res) => {
  const { lat, lng, type, radius } = req.query;
  // radius используется в searchNearbyPlaces
});
```

### Frontend (`/src/services/api.ts`):

Добавили `radius` в preferences:

```typescript
getNearbyPlaces: async (lat, lng, type, preferences?: {
  cuisines?: string[];
  budget?: string;
  interests?: string[];
  radius?: number; // ← NEW
})
```

### MapScreen (`/src/screens/main/MapScreen.tsx`):

```typescript
// State
const [loadedRegions, setLoadedRegions] = useState<Set<string>>(new Set());
const [isLoadingMore, setIsLoadingMore] = useState(false);

// Handler
const handleRegionChangeComplete = (region: Region) => {
  const radius = calculateRadiusFromDelta(region.latitudeDelta);
  const key = getRegionKey(region.latitude, region.longitude, radius);
  
  if (loadedRegions.has(key)) return; // Skip if loaded
  
  loadedRegions.add(key);
  fetchPlaces(activeFilter, newLocation, radius, true); // Append
};
```

---

## 🎯 UI Indicators

### Loading badge:

```tsx
<View style={styles.countBadge}>
  <Text>320 мест</Text>
  <Text>• 306 💚</Text>
  {isLoadingMore && <Text> 🔄</Text>} // ← Spinner
</View>
```

**Отображение:**
- `320 мест • 306 💚` — обычное состояние
- `320 мест • 306 💚 🔄` — подгружаются новые места

---

## 🧪 Тестирование

### Вручную:

1. Откройте карту в Киеве
2. Zoom in максимально → видите 50-100 мест
3. Zoom out → автоматически загружается больше
4. Двигайте карту в стороны → загружаются новые области
5. Вернитесь назад → нет повторной загрузки (кеш работает)

### Логи:

```
🗺️ [MapScreen] Region changed: lat=50.4501, lng=30.5234, delta=0.0500
🔄 [MapScreen] Loading places for new region (3300m)...
🗺️ [MapScreen] Fetching places: radius=3300m, append=true
📍 [MapScreen] API Response: placesCount=180
➕ [MapScreen] Added 120 new places (180 total fetched)
```

---

## ⚙️ Настройки

### Изменить радиус покрытия:

```typescript
// В calculateRadiusFromDelta()
const radiusKm = visibleKm * 0.6; // 60% покрытия

// Увеличить покрытие:
const radiusKm = visibleKm * 0.8; // 80% покрытия

// Уменьшить покрытие (больше подгрузок):
const radiusKm = visibleKm * 0.4; // 40% покрытия
```

### Изменить лимиты радиуса:

```typescript
// Текущие лимиты
Math.min(Math.max(radiusKm * 1000, 1000), 50000)
//                                 ↑      ↑
//                               Min    Max

// Для большей детализации:
Math.min(Math.max(radiusKm * 1000, 500), 20000)
```

### Изменить точность кеширования:

```typescript
// Текущая точность: ~100м
Math.round(lat * 1000) / 1000

// Больше запросов (точнее):
Math.round(lat * 10000) / 10000 // ~10м

// Меньше запросов (грубее):
Math.round(lat * 100) / 100 // ~1км
```

---

## 🎉 Преимущества

1. **Производительность**
   - Загружаются только видимые места
   - Меньше нагрузка на API

2. **UX**
   - Плавная работа при исследовании
   - Нет задержек при zoom
   - Всегда актуальные данные

3. **Масштабируемость**
   - Работает в любом городе
   - Адаптируется под размер viewport
   - Экономит трафик

---

**Готово! Карта теперь умная и подгружает места по мере исследования! 🗺️✨**

