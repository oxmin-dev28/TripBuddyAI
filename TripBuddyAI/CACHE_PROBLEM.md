# 🚨 КРИТИЧНО: Проблема с кэшем

## Проблема
iOS/Expo кэшировал старый код где был `Shadows` и `SHADOW_COLOR`.

Даже после удаления из кода, Metro bundler всё ещё загружает старый JavaScript.

## ✅ РЕШЕНИЕ

### Вариант 1: ПОЛНАЯ ПЕРЕЗАГРУЗКА (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Полностью закрой Expo (Ctrl+C в терминале)

# 2. Полностью закрой приложение на телефоне (свайп вверх, закрыть Expo Go)

# 3. Очисти ВСЁ:
cd TripBuddyAI
rm -rf .expo node_modules/.cache
watchman watch-del-all

# 4. Запусти заново:
npx expo start --clear

# 5. ВАЖНО: На телефоне УДАЛИ приложение Expo Go полностью и установи заново
# Или: В настройках Expo Go → Clear Cache
```

### Вариант 2: Использовать WEB (быстрее для теста)

```bash
cd TripBuddyAI
npx expo start

# Нажми: w
```

Web версия откроется в браузере - там кэш не так агрессивен.

---

## 🔥 Если ничего не помогает

Удали node_modules и переустанови:

```bash
cd TripBuddyAI
rm -rf node_modules
npm install
npx expo start --clear
```

---

## 💡 Почему это происходит?

React Native/Hermes/Metro имеют ОЧЕНЬ агрессивное кэширование:
1. Metro bundler кэш (.expo, node_modules/.cache)
2. Watchman кэш
3. Expo Go app кэш на устройстве
4. iOS DerivedData (если используешь симулятор)

Нужно очистить ВСЕ эти кэши чтобы гарантировать чистую сборку.

---

## 🎯 Попробуй СЕЙЧАС:

1. **Закрой Expo** (Ctrl+C)
2. **Закрой Expo Go на телефоне полностью**
3. **В настройках Expo Go** → найди "Clear cache" или "Clear data"
4. **Запусти снова**: `npx expo start --clear`
5. **Отсканируй QR заново**

Должно заработать! 🚀

