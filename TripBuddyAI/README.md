# ✅ TripBuddy AI - Production Ready

## 🎉 Статус: Готово к запуску!

Приложение настроено в **production режиме** с реальными API:

```
✅ OpenAI API          - AI генерация маршрутов
✅ Google Places API   - Реальные места для всех городов
✅ 35+ городов         - Европа, США, Азия
✅ Умные фильтры       - Кухни, бюджет, интересы
✅ Время работы        - Проверка открыт/закрыт
✅ Локация-aware       - GPS + города назначения
```

---

## 🚀 Быстрый старт

### Команда для запуска (локально в этом репо):

> ℹ️ Репозиторий содержит вложенную папку `TripBuddyAI`. Перед установкой/запуском зайди в неё:
> ```bash
> cd TripBuddyAI
> ```
> Иначе `npx expo start` покажет ошибку `package.json does not exist`.

1) Установи зависимости (один раз):
```bash
npm install          # фронтенд
cd backend && npm install  # backend
```

2) Подними backend в отдельном терминале:
```bash
cd backend
npm run dev
```

3) Запусти Expo (web/эмуляторы/Expo Go) из корня репозитория:
```bash
npm run web          # web предпросмотр
# или: npx expo start  # если нужен QR/эмулятор
```

Подсказки:
- Если Expo попросит аккаунт, выбирай **“Proceed anonymously”**.
- Для QR/эмуляторов в Expo CLI жми: `a` (Android), `i` (iOS), `w` (Web).

---

## 🧪 Проверка работы API

### 1. Статус сервисов:
```bash
curl http://localhost:3001/api/status | python3 -m json.tool
```

**Ожидаемый результат:**
```json
{
    "success": true,
    "services": {
        "openai": true,
        "googlePlaces": true,
        "googlePlacesWorking": true
    },
    "mode": "production",
    "message": "All AI services configured"
}
```

### 2. Тест Google Places (разные города):

**Париж - рестораны:**
```bash
curl "http://localhost:3001/api/places?lat=48.8566&lng=2.3522&type=restaurant"
```

**Рим - достопримечательности:**
```bash
curl "http://localhost:3001/api/places?lat=41.9028&lng=12.4964&type=attraction"
```

**Барселона - кафе:**
```bash
curl "http://localhost:3001/api/places?lat=41.3851&lng=2.1734&type=cafe"
```

**Лондон - музеи:**
```bash
curl "http://localhost:3001/api/places?lat=51.5074&lng=-0.1278&type=museum"
```

### 3. Фильтр по времени работы:
```bash
curl "http://localhost:3001/api/places?lat=48.8566&lng=2.3522&type=restaurant&time=2025-01-15T14:00:00&openOnly=true"
```

---

## 🌍 Поддерживаемые города (35+)

### 🇫🇷 Франция
Париж, Ницца, Лион, Марсель

### 🇮🇹 Италия  
Рим, Милан, Флоренция, Венеция, Неаполь

### 🇪🇸 Испания
Мадрид, Барселона, Севилья, Валенсия

### 🇬🇧 Великобритания
Лондон, Эдинбург, Манчестер

### 🇩🇪 Германия
Берлин, Мюнхен, Гамбург, Франкфурт

### 🇵🇹 Португалия
Лиссабон, Порту

### 🇬🇷 Греция
Афины, Санторини

### 🇳🇱 Нидерланды
Амстердам

### 🇹🇷 Турция
Стамбул, Анталья

### 🇯🇵 Япония
Токио, Киото, Осака

### 🇺🇸 США
Нью-Йорк, Лос-Анджелес, Сан-Франциско, Майами

### 🇹🇭 Таиланд
Бангкок, Пхукет

**Google Places API работает для ВСЕХ городов мира!**  
Нет необходимости создавать моки для каждого города.

---

## 📱 Как работает приложение

### 1️⃣ Onboarding
Пользователь выбирает:
- Кухни (итальянская, французская, японская, etc.)
- Бюджет (низкий, средний, высокий)
- Интересы (музеи, парки, шоппинг, ночная жизнь)

### 2️⃣ Главный экран (Home)
- Показывает **GPS локацию пользователя**
- Отображает **места рядом** с текущим местоположением
- Кнопка "Карта" → открывает карту с местами вокруг

### 3️⃣ Генерация маршрута
- Выбор страны → города
- AI (OpenAI) создаёт персонализированный маршрут
- Учитывает предпочтения пользователя

### 4️⃣ Карта маршрута
- **Главная карта**: GPS локация + места рядом
- **Карта маршрута**: Город назначения + места в нём
- Фильтры: кухни, бюджет, интересы
- Показывает только **открытые** места

### 5️⃣ День за днём
- Просмотр ежедневного маршрута
- Голосование за места
- Редактирование: удалить, добавить, переставить

### 6️⃣ Профиль
- Статистика путешествий
- Сохранённые маршруты
- Настройки предпочтений
- Достижения

---

## 🎯 Ключевые особенности

### ✅ Без демо-данных
- Все места загружаются **напрямую из Google Places API**
- **300-360 мест на город** с использованием pagination
  - 6 типов мест: рестораны, кафе, музеи, парки, достопримечательности, шопинг
  - 3 страницы по 20 мест для каждого типа = 60 × 6 = 360 мест
  - Автоматическое удаление дубликатов
- Работает для **любого города** по координатам
- Нет необходимости в моках (только для dev без API ключа)

### ✅ Умная фильтрация
- По кухне (французская, итальянская, etc.)
- По бюджету ($, $$, $$$, $$$$)
- По времени работы (открыто/закрыто)
- По типу (ресторан, музей, парк, etc.)

### ✅ Локация-aware
- **Home**: Твоя GPS локация
- **Маршрут**: Город назначения
- Динамическая загрузка мест при движении карты

### ✅ Время работы
- Проверка открыт/закрыт для каждого места
- Фильтр "только открытые"
- Актуально для ресторанов и магазинов

---

## 🔧 Конфигурация

### Backend .env
```env
PORT=3001
OPENAI_API_KEY=sk-proj-...     # ✅ Настроен
GOOGLE_PLACES_API_KEY=AIza...  # ✅ Настроен
```

### Где взять API ключи:
- **OpenAI**: https://platform.openai.com/api-keys
- **Google Places**: https://console.cloud.google.com/apis/credentials
  - Enable: Places API, Maps JavaScript API, Geocoding API

---

## 📚 Документация

- **COMMANDS.md** - Все команды для запуска и управления
- **PRODUCTION_GUIDE.md** - Полное руководство по production
- **backend/SETUP.md** - Настройка backend API
- **start-app.sh** - Скрипт автоматического запуска

---

## 🛠️ Tech Stack

**Frontend:**
- React Native + Expo
- TypeScript
- React Navigation
- react-native-maps (Google Maps)
- Context API
- AsyncStorage

**Backend:**
- Node.js + Express
- TypeScript  
- OpenAI API (GPT-4)
- Google Places API
- Nodemon

---

## 🎨 Структура проекта

```
TripBuddyAI/
├── src/
│   ├── screens/           # Экраны приложения
│   │   ├── onboarding/    # 5 экранов онбординга
│   │   └── main/          # Основные экраны
│   ├── components/        # UI компоненты
│   ├── navigation/        # React Navigation
│   ├── store/             # Context API (state)
│   ├── services/          # API вызовы
│   ├── types/             # TypeScript типы
│   └── constants/         # Города, константы
├── backend/
│   ├── src/
│   │   ├── routes/        # API endpoints
│   │   ├── services/      # OpenAI, Google Places
│   │   ├── utils/         # Вспомогательные функции
│   │   ├── types/         # TypeScript типы
│   │   └── data/          # Mock данные (для dev)
│   └── .env               # API ключи
├── start-app.sh           # Автозапуск
├── COMMANDS.md            # Команды
└── PRODUCTION_GUIDE.md    # Production гайд
```

---

## ✨ Что работает прямо сейчас

✅ Backend запущен на `http://localhost:3001`  
✅ OpenAI API подключён  
✅ Google Places API подключён и протестирован  
✅ Работает для всех 35+ городов  
✅ Фильтрация по времени работы  
✅ Умная сортировка мест  
✅ TypeScript скомпилирован без ошибок  

---

## 🚀 Следующие шаги

1. **Запусти Expo** (см. команды выше)
2. **Открой на телефоне** через Expo Go
3. **Пройди онбординг** (выбери предпочтения)
4. **Создай маршрут** для любого города
5. **Проверь карту** - должна показать реальные места

---

## 🎉 Готово!

Приложение полностью настроено и готово к использованию.  
Все API работают в production режиме.  
Поддерживаются ВСЕ города мира через Google Places API.

**Запускай и тестируй! 🚀**
